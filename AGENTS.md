# AGENTS.md — DanceMirror AI 可执行规范

AI 开发代理在操作本仓库前必须阅读并遵守本文件。

本文件用于规定 AI 开发代理的工作方式、开发边界、代码质量和交付要求。

具体产品需求、交互规则、技术方案和验收标准，以 `docs/` 下当前有效文档为准，不在本文件中重复维护完整需求。

---

## 1. 产品概览

DanceMirror 是一个即来即用、移动端优先的 H5 舞蹈视频比对助手。

用户上传老师示范视频和自己的练舞视频，系统完成视频处理、动作比对，并生成可理解、可执行的舞蹈复盘报告。

当前项目已经完成产品原型和初步 Demo，正在从 Mock 演示阶段进入真实功能实现阶段。

当前已经确定的产品边界：

* 无登录注册，即来即用
* 核心流程为：上传 → 校准与分析 → 视频比对 → 复盘报告
* 移动端优先，同时兼容桌面浏览器
* 优先完成真实 AI 复盘闭环
* 不扩展社区、课程、商城、支付等非核心能力
* 当前页面和视觉风格应尽量保留，不重新制作重复 Demo

---

## 2. 需求与文档优先级

执行任何任务前，必须先阅读与任务相关的项目文档。

至少检查：

* `docs/PRD.md`
* `docs/DESIGN_SYSTEM.md`
* `docs/TECH_SPEC.md`
* `docs/AI_SPEC.md`
* `docs/TASKS.md`
* `docs/HARNESS.md`
* `README.md`
* 当前代码与测试
* 当前 Git 状态

如果文档名称或目录发生变化，应主动查找当前有效文件，不得因为路径变化而跳过阅读。

当不同信息发生冲突时，按照以下优先级执行：

1. 用户在当前对话中提出的最新明确要求
2. 当前有效的 PRD 和验收标准
3. 当前有效的 TECH_SPEC、AI_SPEC 和设计说明
4. 本 `AGENTS.md`
5. README 和其他项目文档
6. 现有代码行为

现有代码不代表需求一定正确。

如果现有实现与当前 PRD 冲突，应优先按照当前 PRD 修正，并在交付说明中列出差异。

不要擅自创造与 PRD 冲突的新产品规则。

---

## 3. 工作方式

### 3.1 开始开发前

接到较大开发任务后，先完成项目检查，不要立即重建项目或大规模重写代码。

检查内容包括：

* 当前项目能够完成哪些流程
* 哪些功能仍然使用 Mock、固定结果或占位逻辑
* PRD 与代码之间的差异
* 当前报错、TODO 和技术债
* 项目技术栈、模块边界和依赖
* 前后端接口、模型服务和数据结构
* 测试、构建和部署配置
* 当前 Git 分支、未提交修改和远程仓库
* 修改是否可能覆盖用户现有工作

检查完成后，输出：

1. 对本次任务的理解
2. 当前项目完成度
3. 已完成和未完成能力
4. PRD 与现有实现的主要差异
5. 推荐的开发顺序
6. 预计修改的核心模块
7. 验收和测试方式
8. 需要用户确认的问题

### 3.2 提问规则

只有当缺失信息会明显影响以下事项时，才向用户提问：

* 产品范围
* 技术架构
* 模型服务
* 数据保存
* 隐私与安全
* 部署环境
* 成本
* GitHub 仓库或权限
* 无法逆转的实现决策

提问必须遵守：

* 优先从 PRD、代码、Git 记录和配置中寻找答案
* 不重复询问项目文档中已经明确的内容
* 不重复询问用户已经回答过的问题
* 集中提出问题，通常不超过 8 个
* 按照对开发影响的优先级排序
* 每个问题给出推荐方案和默认方案
* 次要问题不得阻塞核心开发

收到用户回答后，直接继续实施。

除非出现新的重大范围变化、权限阻塞或不可逆风险，否则不要再次要求用户“确认后才能继续”。

### 3.3 小任务

范围明确、风险较低且不涉及产品决策的小任务，可以直接执行，例如：

```text
请按 AGENTS.md 的约束完成 docs/TASKS.md 中的 Task X。
完成后运行 npm run check，并说明实际修改和验证结果。
```

---

## 4. 开发边界

### 应该

* 优先遵守仓库中的当前有效文档
* 基于现有项目继续开发
* 保持移动端优先
* 保持即来即用，不引入登录注册
* 优先完成视频上传、比对和 AI 复盘核心闭环
* 保留已经完成且质量合格的 UI、组件和交互
* 优先修复真实用户流程，再优化次要视觉细节
* 将复杂逻辑拆分为独立模块
* 使用真实数据和真实处理结果驱动界面
* 为加载、空状态、失败、超时和重试提供完整处理
* 对技术无法可靠判断的能力明确标记限制
* 每次修改后运行与修改范围匹配的检查和测试
* 在不阻塞开发的情况下使用合理默认方案，并记录假设

### 不应该

* 擅自改变产品定位
* 重新创建与现有项目重复的新 Demo
* 一次性引入登录、支付、社区和课程系统
* 把穿搭功能扩展成商城
* 把产品扩展成通用运动平台
* 使用泛泛评分替代具体差异解释
* 使用固定 Mock 结果冒充真实 AI 分析
* 使用假进度条冒充真实任务进度
* 让大语言模型在没有结构化数据支撑时凭空判断专业舞蹈问题
* 因为部分 AI 能力暂未实现而阻塞可以独立完成的视频播放、同步和姿态识别功能
* 为了技术炫技大规模更换现有技术栈
* 在没有明确收益的情况下过度设计后端架构
* 覆盖或删除用户已有的未提交修改

---

## 5. Mock 与真实能力迁移

当前项目正在从 Mock 阶段向真实实现迁移。

### Mock 使用规则

* 仍需保留的 Mock 数据必须集中管理
* Mock 数据必须明确标记，不能与真实结果混淆
* 不得在多个组件中复制相同 Mock 数据
* 不得使用随机数据冒充真实分析结果
* 正式功能完成后，应删除或隔离对应 Mock 实现
* 如需保留演示模式，应通过清晰的配置或环境变量控制

### 真实实现规则

* 页面展示的数据必须来自真实处理结果或明确的降级结果
* 视频、姿态关键点、时间轴和报告数据应使用清晰的数据结构
* 前端不得直接包含服务端模型密钥
* 模型调用、视频处理和敏感配置应由受控后端处理
* 语言模型输出应基于结构化分析结果，而不是直接凭整段视频生成结论

---

## 6. 代码规范

### 当前基础结构

```text
index.html          — 单页面入口
styles.css          — 全局样式
app.js              — 当前应用入口与基础逻辑
docs/               — 产品工程文档
docs/archive/       — 文档历史版本
tests/              — 测试
tests/contract/     — Contract Test
tests/unit/         — Unit Test
AGENTS.md           — AI 可执行规范
```

以上是当前基础结构，不代表后续所有功能都必须继续放在单个文件中。

随着真实能力接入，可以按照 `TECH_SPEC.md` 合理增加模块，例如：

```text
src/
  components/       — 页面和交互组件
  services/         — API 与模型服务调用
  media/            — 视频、音频和时间轴处理
  pose/             — 姿态识别与关键点处理
  comparison/       — 动作对齐和结构化指标
  report/           — 报告转换与展示
  state/            — 应用状态管理
server/             — 后端服务，按 TECH_SPEC 决定是否使用
```

不要为了满足该示例而强行重构。

只有当现有结构已经阻碍功能实现、测试或维护时，才进行模块拆分。

### 命名

* JS 函数：camelCase
* CSS 类：kebab-case
* 文件：kebab-case
* 常量：UPPER_SNAKE_CASE

### JS 规范

* ESLint recommended
* 2 空格缩进
* 优先 `const`，其次 `let`
* 不使用 `var`
* 单引号
* 无分号
* 避免超长函数和重复逻辑
* 复杂时间轴、同步和分析逻辑必须拆分为可测试函数

### CSS 规范

* Stylelint standard
* 2 空格缩进
* CSS 变量定义在 `:root`
* 移动端优先
* `@media` 按宽度递增
* 避免无必要的固定宽高
* 不得造成移动端横向滚动

### HTML 规范

* 必须设置 `lang="zh-CN"`
* 必须包含 viewport meta
* 使用语义化标签
* 交互控件必须支持键盘操作和可访问标签

---

## 7. AI 与舞蹈分析边界

所有 AI 反馈必须尽量建立在可验证数据之上。

可以作为事实或计算结果输出的内容包括：

* 关键点位置
* 关节角度
* 时间差
* 节拍偏差
* 动作幅度
* 运动轨迹
* 躯干倾斜
* 左右协调差异
* 关键点置信度

对于以下暂时无法通过现有数据可靠判断的概念，应谨慎处理：

* 发力方式
* 肌肉控制
* rolling 质感
* 中段稳定性
* 柔韧度
* 舞感和表现力

当输出包含推测时，必须明确说明：

* 该结论是计算结果还是推测
* 依据的数据
* 当前置信度
* 可能的误差来源
* 用户可以如何验证

不得把画面位置偏移直接解释为舞蹈动作错误。

不得伪装成已经具备实际不存在的舞蹈理解能力。

---

## 8. 测试规范

### 测试分层

| 层级               | 位置                   | 内容                    | 运行时机    |
| ---------------- | -------------------- | --------------------- | ------- |
| Contract Test    | `tests/contract/`    | 验证前后端和 AI 输出符合 schema | 每次相关修改  |
| Unit Test        | `tests/unit/`        | 纯函数、工具函数和算法           | 每次相关修改  |
| Integration Test | `tests/integration/` | 模块间流程                 | 涉及真实接口时 |
| E2E Test         | `tests/e2e/`         | 核心用户流程                | 具备条件时   |

不要求为了测试数量强行增加无价值测试。

### Contract Test

报告结果应符合当前 `docs/AI_SPEC.md` 中定义的 schema。

如果 AI_SPEC 已更新，应以最新 schema 为准，不得继续使用本文件中的旧字段定义覆盖 AI_SPEC。

基础检查应包括：

* 必填字段存在
* 字段类型正确
* 时间戳格式正确
* 不匹配项能够定位到视频时间
* 建议内容非空
* 训练计划数据结构正确
* 前后端 schema 一致
* 无无法序列化的数据

### 基础命令

```bash
npm run test
npm run check
```

如果项目增加了类型检查、构建或 E2E 命令，也必须根据修改范围执行，例如：

```bash
npm run typecheck
npm run build
npm run test:e2e
```

### 手动检查

至少检查：

* 页面是否可以正常打开
* 移动端是否无横向滚动
* 老师视频和用户视频是否可以独立上传
* 删除和重新上传是否可用
* 视频播放、暂停和拖动是否正常
* loading、error、empty 和 retry 状态是否存在
* 分析结果是否符合 AI_SPEC
* 浏览器控制台是否存在阻塞使用的报错

---

## 9. 质量门槛

功能不能只以“代码已经生成”作为完成标准。

每个功能完成后必须满足：

* 实际用户流程可操作
* 代码已经写入项目文件
* 核心逻辑不是固定 Mock
* 错误和异常状态可处理
* 相关测试通过
* `npm run check` 通过
* 生产构建可以完成
* 文档与实际实现一致
* 未完成能力被明确标记
* 没有将密钥或敏感数据提交到仓库

如果某项检查无法通过，必须说明：

* 失败的命令
* 具体错误
* 已采取的处理
* 当前剩余风险
* 用户可以执行的下一步命令

不得将未验证的功能描述为已完成。

---

## 10. 文案约束

所有用户反馈和 AI 建议必须：

* 具体
* 温和
* 可执行
* 有数据依据或明确说明属于推测
* 不攻击身材
* 不贬低用户能力
* 不做医疗诊断
* 不使用无法解释的专业术语堆砌
* 优先指出最值得改进的 1～3 个问题

---

## 11. 安全与环境变量

* API Key 只能通过环境变量或安全配置注入
* 前端不得暴露服务端密钥
* 不得提交 `.env`
* 必须提供 `.env.example`
* 不得提交用户上传的视频
* 不得提交日志、缓存和临时处理文件
* 上传文件必须校验类型、大小和时长
* 外部请求必须处理超时和失败
* 日志中不得打印完整密钥和敏感数据
* 如果需要远程文件存储，应按照 TECH_SPEC 实现，不得自行引入未经确认的高成本服务

---

## 12. Git 与版本管理

### 分支策略

```text
main (受保护，只接受 PR)
 ├── develop (日常开发基线)
 │    ├── feature/task-1-xxx
 │    ├── feature/task-2-xxx
 │    └── ...
 ├── release/v0.3
 └── hotfix/xxx
```

* `main`：受保护，禁止直接 push，必须通过 PR
* `develop`：日常开发基线
* `feature/<task-id>-<简述>`：功能分支
* `release/v<版本号>`：发版分支
* `hotfix/<简述>`：紧急修复分支

执行 Git 操作前必须：

* 检查当前分支
* 检查未提交修改
* 检查远程仓库
* 避免覆盖用户现有修改
* 确认目标分支符合分支策略

禁止：

* 未经授权使用 `git reset --hard`
* 未经授权使用强制推送
* 删除用户已有有效分支
* 提交环境变量、密钥、上传视频和大体积临时文件
* 直接推送到受保护的 `main`

### Commit Message

```text
<type>(<scope>): <subject>
```

type：

* `feat`：新功能
* `fix`：修复
* `docs`：文档
* `style`：样式
* `refactor`：重构
* `test`：测试
* `chore`：构建或工具

scope：

* `upload`
* `player`
* `sync`
* `pose`
* `compare`
* `report`
* `api`
* `docs`
* `harness`
* `deploy`

示例：

```text
feat(upload): 支持老师与用户视频独立上传
fix(player): 修复播放按钮只控制最后一个视频
feat(sync): 增加双视频时间轴校准
feat(pose): 接入真实姿态关键点识别
docs(prd): 更新真实分析阶段验收标准
chore(deploy): 增加生产部署配置
```

### Tag

* 格式：`v0.1.0`、`v0.2.0`
* 只在 `main` 分支打 tag
* 每个 release 合并到 `main` 后打 tag

### 文档版本

* `docs/` 中存放当前有效版本
* 文件名不携带版本号
* 重大变更前将旧版本归档到 `docs/archive/`
* 文档头部维护版本号、状态和 changelog
* 不要同时维护多个互相冲突的“当前版本”

---

## 13. GitHub 与部署

完成开发后，在权限和部署条件具备时，应继续完成 GitHub 推送和部署，不应只停留在本地代码修改。

### GitHub

* 按分支策略创建或使用功能分支
* 生成清晰的 commit
* 推送至正确远程仓库
* 创建 PR 合并至 `develop` 或对应目标分支
* 不直接绕过 `main` 的保护规则
* 在 PR 中说明功能、测试结果和已知限制

### 部署

部署方式应以当前技术架构为准。

* 纯静态前端可以使用 GitHub Pages 或静态托管平台
* 包含后端接口、模型调用、文件处理或数据库时，不得将 GitHub Pages 作为完整部署方案
* GitHub 可以用于代码托管和 CI/CD
* 前端、后端、存储和模型服务可以分别部署
* 优先复用仓库中已经存在的部署配置

部署前应检查：

* 构建命令
* 启动命令
* 环境变量
* 跨域配置
* 文件存储
* 模型服务
* API 地址
* 域名和 HTTPS
* 移动端访问

部署完成后必须进行基础冒烟测试，并提供实际访问地址。

如果因为账号、权限、密钥或平台登录无法完成部署，应明确说明唯一的阻塞项，不得笼统描述为“无法部署”。

---

## 14. 文档维护

当代码、配置、架构或使用方式发生变化时，需要同步更新：

* README
* `.env.example`
* TECH_SPEC
* AI_SPEC
* TASKS
* HARNESS
* 启动和构建命令
* 部署说明
* 已知限制

如果 PRD 已经包含具体产品需求，不要将完整需求重复复制到 AGENTS.md。

AGENTS.md 只维护长期稳定的代理执行规则。

---

## 15. 版本节奏

* v0.1：静态 H5 原型，Mock AI 报告
* v0.2：即来即用 H5，核心流程与交互完善
* v0.3：H5 + 后端 API 与任务处理
* v0.4：真实视频抽帧、姿态识别与结构化分析 POC
* v0.5：双视频校准、动作对齐和真实 AI 报告闭环
* v0.6：微信小程序测试版

具体版本范围以当前 PRD、TASKS 和 release 计划为准。

---

## 16. 任务完成后的输出

完成较大任务后，必须输出可验证的交付说明：

1. 实际完成的功能
2. 修改的核心文件和模块
3. 修复的问题
4. 测试命令和结果
5. 构建结果
6. 未执行的测试及原因
7. Git 分支和 commit
8. PR 地址
9. 部署地址
10. 环境变量和启动方式
11. 已知限制
12. 下一阶段建议

所有“已完成”结论必须有代码、命令、测试结果、Git 记录或线上地址作为依据。

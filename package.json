{
  "name": "dancemirror",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "AI舞蹈视频比对助手 - 即来即用H5",
  "scripts": {
    "lint": "eslint app.js ai.js server.js components hooks services && htmlhint index.html && stylelint styles.css",
    "test": "vitest run",
    "check": "npm run lint && npm run test",
    "serve": "node server.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^17.7.0",
    "htmlhint": "^1.1.4",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
